import logging
logging.basicConfig(level=logging.DEBUG, format="%(levelname)s:%(message)s")
logging.debug('This message should appear on the console, example1')
logging.info('So should this, example1')
logging.warning('And this, too, example1')